name: Semantic Versioning and Release

on:
  push:
    branches:
      - main
    
  workflow_dispatch:

jobs:
  semantic-version:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    outputs:
      new_version: ${{ steps.semver.outputs.version }}
      version_changed: ${{ steps.semver.outputs.version_changed }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Calculate and tag semantic version
        id: semver
        uses: PaulHatch/semantic-version@v5.4.0
        with:
          tag_prefix: "v"
          major_pattern: "(BREAKING CHANGE|breaking:|major:)"
          minor_pattern: "(feat|feature|minor:)"
          version_format: "${major}.${minor}.${patch}"
          bump_each_commit: false
          search_commit_body: true
          user_api_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Debug version info
        run: |
          echo "Version: ${{ steps.semver.outputs.version }}"
          echo "Version Changed: ${{ steps.semver.outputs.version_changed }}"
          echo "Tag: ${{ steps.semver.outputs.tag }}"

  build-and-push:
    needs: semantic-version
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata with semver
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=semver,pattern={{version}},value=${{ needs.semantic-version.outputs.new_version }}
            type=semver,pattern={{major}}.{{minor}},value=${{ needs.semantic-version.outputs.new_version }}
            type=semver,pattern={{major}},value=${{ needs.semantic-version.outputs.new_version }}
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=sha,prefix=sha-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push image to GHCR
        id: image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: Image pushed successfully
        run: |
          echo "✅ Docker image successfully built and pushed to GHCR"
          echo "📦 Image Repository: ghcr.io/${{ github.repository }}"
          echo "🏷️  New Version: ${{ needs.semantic-version.outputs.new_version }}"
          echo "📋 All tags:"
          echo "${{ steps.meta.outputs.tags }}"

  create-release:
    needs: [semantic-version, build-and-push]
    runs-on: ubuntu-latest
    
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ needs.semantic-version.outputs.new_version }}
          name: Release v${{ needs.semantic-version.outputs.new_version }}
          body: |
            ## Docker Image
            
            **Repository:** `ghcr.io/${{ github.repository }}`
            
            **Tags:**
            - `${{ needs.semantic-version.outputs.new_version }}`
            - `latest`
            
            ### Pull the image:
            ```bash
            docker pull ghcr.io/${{ github.repository }}:${{ needs.semantic-version.outputs.new_version }}
            ```
            
            ### Deploy with Helm:
            ```bash
            helm install my-release ./helm/go-htmx-fiber \
              --set image.tag=${{ needs.semantic-version.outputs.new_version }}
            ```
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
