# Example SecretStore for different backends
# Uncomment and configure the one that matches your secret storage system

---
# Option 1: GitHub Secrets (via API)
# apiVersion: external-secrets.io/v1beta1
# kind: SecretStore
# metadata:
#   name: github-secret-store
#   namespace: {{ .Release.Namespace }}
# spec:
#   provider:
#     github:
#       auth:
#         secretRef:
#           secretAccessKey:
#             name: github-token
#             key: token
#       owner: igorsilva-dev
#       repo: go_htmx_fiber_fullstack

---
# Option 2: AWS Secrets Manager
# apiVersion: external-secrets.io/v1beta1
# kind: SecretStore
# metadata:
#   name: aws-secret-store
#   namespace: {{ .Release.Namespace }}
# spec:
#   provider:
#     aws:
#       service: SecretsManager
#       region: us-east-1
#       auth:
#         jwt:
#           serviceAccountRef:
#             name: external-secrets-sa

---
# Option 3: Vault
# apiVersion: external-secrets.io/v1beta1
# kind: SecretStore
# metadata:
#   name: vault-secret-store
#   namespace: {{ .Release.Namespace }}
# spec:
#   provider:
#     vault:
#       server: "https://vault.example.com:8200"
#       path: "secret"
#       auth:
#         kubernetes:
#           mountPath: "kubernetes"
#           role: "external-secrets"

---
# Option 4: Kubernetes Secret (local, for development)
# apiVersion: external-secrets.io/v1beta1
# kind: SecretStore
# metadata:
#   name: kubernetes-secret-store
#   namespace: {{ .Release.Namespace }}
# spec:
#   provider:
#     kubernetes:
#       server:
#         url: "https://kubernetes.default.svc"
#       auth:
#         serviceAccount:
#           name: external-secrets-sa
